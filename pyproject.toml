[project]
name = "comfyui-attention-optimizer"
description = "Automatically benchmark and optimize attention in diffusion models. 1.5-2x speedup on RTX 4090, up to 4x on video models."
version = "1.0.0"
license = { file = "LICENSE" }
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.urls]
Repository = "https://github.com/D-Ogi/ComfyUI-Attention-Optimizer"

[tool.comfy]
PublisherId = "dogi"
DisplayName = "Attention Optimizer"
Icon = "https://raw.githubusercontent.com/D-Ogi/ComfyUI-Attention-Optimizer/main/icon.svg"
